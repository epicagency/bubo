/**
 * Check if invalid tel
 *
 * @export
 * @param {string} value email value
 * @returns {false} always false
 */
export default function isNotTel() {
  // No standalone validation
  // this must be done with pattern and/or min/maxlength
  return false;
}
